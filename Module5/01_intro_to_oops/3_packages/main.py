# combination of certain module leads us to a package
# like in maths book(package)- there are certain chapters (modules) like mensuration, differentuiation, integration, trigonometry
# mensuration itself can be a package having different modules like circle, sphere etc


from mensuration import circle
print(circle.area(4))
from mensuration import  square
print(square.circumference((4)))

# go to terminal and write python main.py to run
